import { formatNumber, type LearningProgram } from "@/features/mis/mock-service";
export function CourseEngagement({ programs }: { programs: LearningProgram[] }) { return <div className="space-y-6">{programs.map(program => <div key={program.id}><div className="mb-2 flex items-end justify-between"><div><p className="text-sm font-semibold">{program.name}</p><p className="mt-0.5 text-[10px] text-muted-foreground">{formatNumber(program.enrolledStudents)} students</p></div><p className="text-sm font-bold">{program.engagement}%</p></div><div className="h-2 overflow-hidden rounded-sm bg-muted"><div className={`h-full bg-${program.color}`} style={{ width: `${program.engagement}%` }} /></div></div>)}</div>; }
