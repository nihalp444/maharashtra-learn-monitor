import { ArrowDownRight, ArrowUpRight, type LucideIcon } from "lucide-react";
import { formatNumber } from "@/features/mis/mock-service";
export function KpiCard({ label, value, suffix, change, icon: Icon, tone = "blue" }: { label: string; value: number; suffix?: string; change: number; icon: LucideIcon; tone?: string }) {
  const positive = change >= 0;
  return <article className="rounded-sm border border-border bg-card px-3 py-3 shadow-mis transition-colors hover:border-input"><div className="flex items-center gap-3"><div className={`grid size-8 shrink-0 place-items-center rounded-sm bg-${tone}-soft text-${tone}`}><Icon className="size-4" /></div><div className="min-w-0"><p className="truncate text-[10px] font-semibold text-muted-foreground">{label}</p><p className="mt-0.5 text-xl font-bold leading-none tabular-nums text-card-foreground">{suffix === "%" ? value.toFixed(1) : formatNumber(value)}{suffix}</p></div></div><div className="mt-2 flex items-center justify-between gap-1 border-t border-border pt-2"><span className={`inline-flex items-center text-[10px] font-semibold ${positive ? "text-positive" : "text-destructive"}`}>{positive ? <ArrowUpRight className="size-3" /> : <ArrowDownRight className="size-3" />}{Math.abs(change)}%</span><span className="truncate text-[9px] text-muted-foreground">vs previous period</span></div></article>;
}
